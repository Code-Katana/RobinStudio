import { ChannelNames } from "@shared/types";

export const browserWindowActions: ChannelNames = {
  closeWindow: "close-window",
  minimizeWindow: "minimize-window",
  maximizeWindow: "maximize-window",
};
